{
  "name": "expense-tracker-line-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types scripts/test-flow.mjs && node --experimental-strip-types scripts/chat-engine-check.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260811.1",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2",
    "wrangler": "^4.120.1"
  }
}
